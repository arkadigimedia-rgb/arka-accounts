"use client";
import { useEffect, useMemo, useState } from "react";
import { ArrowUpRight, CalendarClock, CheckCircle2, CircleAlert, FileSpreadsheet, IndianRupee, Users } from "lucide-react";
type Payment={id:number;client:string;service:string;expectedAmount:number;dueDate:string;status:string;nextAction?:string};
const money=(n:number)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(n);
const nice=(v:string)=>v.split("_").map(x=>x[0]+x.slice(1).toLowerCase()).join(" ");
export default function Workspace(){const [items,setItems]=useState<Payment[]>([]);const [month,setMonth]=useState(new Date().toISOString().slice(0,7));const [notice,setNotice]=useState("Loading live payment data…");useEffect(()=>{fetch("/api/payments").then(r=>r.json()).then(rows=>{if(Array.isArray(rows)){setItems(rows);setNotice("Payment ledger is ready.")}else setNotice("Google Sheet is not connected yet.")}).catch(()=>setNotice("Payment data is unavailable."))},[]);const rows=useMemo(()=>items.filter(p=>p.dueDate.startsWith(month)),[items,month]);const due=rows.filter(p=>["OVERDUE","DUE_TODAY"].includes(p.status));const pending=rows.filter(p=>["PROOF_UPLOADED","VERIFYING","MANUAL_REVIEW","MISMATCH"].includes(p.status));const total=rows.reduce((s,p)=>s+p.expectedAmount,0);const clients=new Set(rows.map(p=>p.client)).size;return <main className="min-h-screen bg-[#f4f6f8] text-slate-950"><header className="border-b bg-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5 sm:px-8"><div className="flex items-center gap-3"><b className="grid h-10 w-10 place-items-center rounded-xl bg-[#fbbf24] text-lg">A</b><div><p className="font-bold tracking-[.2em]">ARKA</p><p className="text-xs text-slate-500">PAYMENT OPERATIONS</p></div></div><a href="/monthly-billing" className="rounded-xl bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white">Monthly billing</a></div></header><section className="mx-auto max-w-7xl px-5 py-7 sm:px-8"><div className="flex flex-col justify-between gap-4 lg:flex-row lg:items-end"><div><p className="text-sm font-semibold text-amber-700">LIVE FINANCE WORKSPACE</p><h1 className="mt-2 text-3xl font-bold tracking-tight sm:text-4xl">Know every payment that needs attention.</h1><p className="mt-3 max-w-2xl text-base leading-7 text-slate-600">Your Google Sheet becomes the source of truth for clients, invoices, due dates, balances, and payment follow-up.</p></div><label className="rounded-xl border bg-white px-3 py-2 text-sm font-medium">Billing month <input type="month" value={month} onChange={e=>setMonth(e.target.value)} className="ml-2 outline-none"/></label></div><div className="mt-6 rounded-2xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-medium text-emerald-900">{notice}</div><div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Card icon={IndianRupee} label="Expected this month" value={money(total)}/><Card icon={Users} label="Active clients" value={String(clients)}/><Card icon={CalendarClock} label="Due or overdue" value={String(due.length)} warn/><Card icon={CircleAlert} label="Verification queue" value={String(pending.length)} warn/></div><div className="mt-6 grid gap-6 lg:grid-cols-[1.5fr_.8fr]"><section className="overflow-hidden rounded-2xl border bg-white"><div className="flex items-center justify-between border-b px-5 py-4"><div><h2 className="font-bold">Monthly payment ledger</h2><p className="mt-1 text-sm text-slate-500">Payments from the selected sheet month</p></div><a href="/monthly-billing" className="text-sm font-semibold text-amber-800">Manage invoices</a></div>{rows.length?<div className="overflow-x-auto"><table className="w-full min-w-[650px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3">Client</th><th>Service</th><th>Amount</th><th>Due date</th><th className="pr-5">Status</th></tr></thead><tbody>{rows.map(p=><tr key={p.id} className="border-t"><td className="px-5 py-4 font-semibold">{p.client}</td><td>{p.service}</td><td>{money(p.expectedAmount)}</td><td>{p.dueDate}</td><td className="pr-5"><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold">{nice(p.status)}</span></td></tr>)}</tbody></table></div>:<div className="p-12 text-center text-sm text-slate-500">No records for this month. Connect the sheet to see your live work.</div>}</section><aside className="space-y-5"><section className="rounded-2xl bg-slate-950 p-5 text-white"><p className="text-sm font-semibold text-amber-300">NEXT ACTION</p><p className="mt-3 text-lg font-bold">{due[0]?`${due[0].client} needs payment follow-up.`:"No urgent payments today."}</p><p className="mt-2 text-sm leading-6 text-slate-300">Due dates and status update automatically from your monthly invoice rows.</p></section><section className="rounded-2xl border bg-white p-5"><div className="flex items-center gap-2"><FileSpreadsheet className="h-5 w-5 text-emerald-700"/><h2 className="font-bold">Sheet connection</h2></div><ol className="mt-4 space-y-3 text-sm leading-6 text-slate-600"><li>1. Share your Google Sheet link.</li><li>2. Keep your monthly tabs in the same invoice layout.</li><li>3. Add new rows each month or use Monthly Billing.</li></ol><a href="/monthly-billing" className="mt-5 inline-flex items-center gap-2 text-sm font-bold text-amber-800">View monthly setup <ArrowUpRight className="h-4 w-4"/></a></section></aside></div><section className="mt-6 rounded-2xl border bg-white p-5"><div className="flex items-center gap-2"><CheckCircle2 className="h-5 w-5 text-emerald-700"/><h2 className="font-bold">What the system reads from your Sheet</h2></div><p className="mt-3 text-sm leading-7 text-slate-600">Client Name, Contact Person, Phone, Email, Service, Billing Type, Billing From, Billing To, Amount, Due Date, and Payment Reference. Your existing monthly tabs can stay in place; the same invoice details will appear here after sync.</p></section></section></main>}
function Card({icon:Icon,label,value,warn=false}:{icon:typeof IndianRupee;label:string;value:string;warn?:boolean}){return <div className="rounded-2xl border bg-white p-5"><Icon className={"h-5 w-5 "+(warn?"text-amber-700":"text-slate-700")}/><p className="mt-4 text-sm text-slate-500">{label}</p><p className="mt-2 text-3xl font-bold">{value}</p></div>}
